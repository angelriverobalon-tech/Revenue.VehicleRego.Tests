name: Test Automation CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read
  checks: write
  pull-requests: write
  actions: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    defaults:
      run:
        working-directory: ./Revenue.Tests.VehicleRego.BDD
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup .NET 8
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
    
    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: ${{ runner.os }}-nuget-
    
    - name: Cache Playwright browsers
      uses: actions/cache@v4
      with:
        path: ~/.cache/ms-playwright
        key: playwright-chromium-1.58.0
        restore-keys: playwright-chromium-
    
    - name: Create env.json for CI
      run: |
        # Create env.json in project root
        echo '{
          "PLAYWRIGHT_URL": "https://www.service.nsw.gov.au/transaction/check-motor-vehicle-stamp-duty",
          "PLAYWRIGHT_HEADLESS": "true",
          "PLAYWRIGHT_BROWSER": "chromium",
          "API_BASE_URL": "https://openlibrary.org/authors/OL1A.json"
        }
        EOF
        cat bin/Release/net8.0/env.json
    
    - name: Verify URL is accessible
      run: |
        echo "Testing URL accessibility..."
        RESPONSE=$(curl -sL -w "%{http_code}" "https://www.service.nsw.gov.au/transaction/check-motor-vehicle-stamp-duty" -o /dev/null)
        echo "HTTP Response Code: $RESPONSE"
        if [ "$RESPONSE" -eq "200" ] || [ "$RESPONSE" -eq "301" ] || [ "$RESPONSE" -eq "302" ]; then
          echo "✅ URL is accessible"
        fi
    
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build project
      run: dotnet build --no-restore --configuration Release
    
    - name: Copy env.json to build output
      run: |
        # Ensure env.json is in the build output directory
        cp env.json bin/Release/net8.0/ || true
        echo "=== env.json location ==="
        find . -name "env.json" -type f
        echo "=== env.json content ==="
        cat bin/Release/net8.0/env.json || echo "File not found"
    
    - name: Install Playwright CLI
      run: dotnet tool install --global Microsoft.Playwright.CLI
    
    - name: Install Playwright browsers
      run: playwright install chromium --with-deps
    
    - name: Run Tests
      run: |
        dotnet test \
          --no-build \
          --configuration Release \
          --logger trx \
          --logger "console;verbosity=minimal"
      continue-on-error: true
      timeout-minutes: 10
    
    - name: List files for debugging
      if: always()
      run: |
        echo "=== Current Directory ==="
        pwd
        echo ""
        echo "=== Looking for screenshots ==="
        find . -name "*.png" -type f || echo "No PNG files found"
        echo ""
        echo "=== Looking for test results ==="
        find . -name "*.trx" -type f || echo "No TRX files found"
    
    - name: Upload Screenshots
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-screenshots
        path: '**/screenshots/**/*.png'
        retention-days: 30
        if-no-files-found: ignore
    
    - name: Upload Test Results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-results
        path: '**/TestResults/**/*.trx'
        retention-days: 30
        if-no-files-found: warn
    
    - name: Create Test Summary with Screenshots
      if: always()
      shell: bash
      run: |
        echo "## 📊 Test Execution Report" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Date:** $(date)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Parse TRX files for test results
        if [ -d "TestResults" ]; then
          TRX_FILES=$(find TestResults -name "*.trx" -type f)
          
          if [ ! -z "$TRX_FILES" ]; then
            # Extract test counts from TRX
            TOTAL=0
            PASSED=0
            FAILED=0
            
            for trx in $TRX_FILES; do
              if [ -f "$trx" ]; then
                T=$(grep -oP 'total="\K[0-9]+' "$trx" | head -1 || echo 0)
                P=$(grep -oP 'passed="\K[0-9]+' "$trx" | head -1 || echo 0)
                F=$(grep -oP 'failed="\K[0-9]+' "$trx" | head -1 || echo 0)
                TOTAL=$((TOTAL + T))
                PASSED=$((PASSED + P))
                FAILED=$((FAILED + F))
              fi
            done
            
            # Display test statistics
            echo "### 📈 Test Statistics" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Metric | Count | Percentage |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|-----------|" >> $GITHUB_STEP_SUMMARY
            echo "| **Total Tests** | $TOTAL | 100% |" >> $GITHUB_STEP_SUMMARY
            
            if [ $TOTAL -gt 0 ]; then
              PASS_PCT=$((PASSED * 100 / TOTAL))
              echo "| ✅ **Passed** | $PASSED | ${PASS_PCT}% |" >> $GITHUB_STEP_SUMMARY
              
              if [ $FAILED -gt 0 ]; then
                FAIL_PCT=$((FAILED * 100 / TOTAL))
                echo "| ❌ **Failed** | $FAILED | ${FAIL_PCT}% |" >> $GITHUB_STEP_SUMMARY
              fi
            fi
            
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # List all test results
            echo "### 📋 Test Results Details" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Status | Test Name | Duration |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-----------|----------|" >> $GITHUB_STEP_SUMMARY
            
            for trx in $TRX_FILES; do
              if [ -f "$trx" ]; then
                while IFS= read -r line; do
                  if [[ $line == *'<UnitTestResult'* ]]; then
                    TESTNAME=$(echo "$line" | grep -oP 'testName="\K[^"]+' || echo "Unknown")
                    OUTCOME=$(echo "$line" | grep -oP 'outcome="\K[^"]+' || echo "Unknown")
                    DURATION=$(echo "$line" | grep -oP 'duration="\K[^"]+' | sed 's/0://' || echo "N/A")
                    
                    if [ "$OUTCOME" = "Passed" ]; then
                      STATUS="✅"
                    elif [ "$OUTCOME" = "Failed" ]; then
                      STATUS="❌"
                    else
                      STATUS="⏭️"
                    fi
                    
                    CLEAN_NAME=$(echo "$TESTNAME" | sed 's/.*\.//' | sed 's/(.*//')
                    
                    echo "| $STATUS | \`$CLEAN_NAME\` | $DURATION |" >> $GITHUB_STEP_SUMMARY
                  fi
                done < "$trx"
              fi
            done
            
            echo "" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ No TRX files found in TestResults" >> $GITHUB_STEP_SUMMARY
          fi
        else
          echo "⚠️ TestResults directory not found" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "---" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        # Screenshots section
        SCREENSHOT_COUNT=$(find . -name "FAILED_*.png" -type f 2>/dev/null | wc -l)
        
        if [ "$SCREENSHOT_COUNT" -gt 0 ]; then
          echo "### 📸 Failure Screenshots: $SCREENSHOT_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Screenshot | Test |" >> $GITHUB_STEP_SUMMARY
          echo "|------------|------|" >> $GITHUB_STEP_SUMMARY
          
          find . -name "FAILED_*.png" -type f 2>/dev/null | while read screenshot; do
            filename=$(basename "$screenshot")
            testname=$(echo "$filename" | sed 's/FAILED_//' | sed 's/_[0-9]*\.png$//' | sed 's/_/ /g')
            echo "| 🖼️ \`$filename\` | $testname |" >> $GITHUB_STEP_SUMMARY
          done
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📥 **Download 'test-screenshots' artifact to view images**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
        fi
        
        # Artifacts section
        echo "### 📦 Artifacts" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- 📥 **test-results** - TRX test result files" >> $GITHUB_STEP_SUMMARY
        if [ "$SCREENSHOT_COUNT" -gt 0 ]; then
          echo "- 📥 **test-screenshots** - Screenshots from failed tests" >> $GITHUB_STEP_SUMMARY
        fi